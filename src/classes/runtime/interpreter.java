package classes.runtime;

import classes.Interfaces.Statement;
import classes.runtime.types.RuntimeVal;

public class interpreter {

    public RuntimeVal evaluate(Statement astNode) {
        switch (astNode.getKind()) {

        }

        return null;
    }

}
